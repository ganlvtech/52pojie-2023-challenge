# 生成频域水印图像

1. 第一步：使用一个 512x512 黑白图像 flag 作为模长，和一个 512x512 随机的噪声图像作为辐角，合成得到一个复数频域的图像。
2. 对这个复数频域的图像进行傅里叶反变换。得到空间域的复数的图像。
3. 分别保存实部和虚部。

说明：使用随机噪声作为辐角是为了避免所有频率分量相位相同，集中在某一个位置叠加产生尖峰。

## 使用方法

安装 Rust 之后，在此文件夹中执行

```bash
cargo run --release
```
